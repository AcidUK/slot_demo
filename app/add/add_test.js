'use strict';

describe('slotDemo.add module', function() {

  beforeEach(module('slotDemo.add'));

  describe('add controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addCtrl = $controller('AddCtrl');
      expect(addCtrl).toBeDefined();
    }));

  });
});